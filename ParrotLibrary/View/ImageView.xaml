<Page x:Class="ParrotLibrary.View.ImageView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ParrotLibrary.View"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ImageView">

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="Auto"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        
        <Button Click="CloseButton_Click" Grid.Column="1" Margin="20,5" FontSize="40" 
                Background="{x:Null}" BorderThickness="0" x:Name="CloseButton" 
                Content="X"  Width="Auto" Height="Auto">
            
        </Button>
        <Image Grid.ColumnSpan="2"  Grid.Row="2" x:Name="ImageData" Stretch="Uniform" Margin="0,0,0,40"></Image>
        
    </Grid>
</Page>
